apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: analytics-network-delay
  namespace: default
spec:
  action: delay
  mode: all
  selector:
    namespaces:
      - default
    labelSelectors:
      app: analytics
  delay:
    latency: "500ms"
  duration: "5m"
